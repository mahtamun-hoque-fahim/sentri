# SITETREE.md — Sentri

> Generated by tree-man after BRAIN.md. Maintained going forward by repo-maintainer's "update repo" trigger.
> Do not hand-edit without updating both halves below.
> API routes do not belong here — those stay in PLANNER.md's "API Routes" table.

---

## Route Tree

```
/                                    (public)
├── /signin                          (public)
├── /signup                          (public)
├── /share/[token]                   (public)
│
├── /unlock                          (authenticated)
├── /onboarding                      (authenticated)
│
├── /dashboard                       (authenticated)
│   ├── /dashboard/[id]              (authenticated)
│   ├── /dashboard/generator         (authenticated)
│   ├── /dashboard/watchtower        (authenticated)
│   ├── /dashboard/sharing           (authenticated)
│   ├── /dashboard/circle            (authenticated)
│   ├── /dashboard/perks             (authenticated)
│   ├── /dashboard/import            (authenticated)
│   ├── /dashboard/emergency-kit     (authenticated)
│   └── /dashboard/settings          (authenticated)
│       └── /dashboard/settings/security  (authenticated)
│
└── /admin                           (admin)
    ├── /admin/users                 (admin)
    │   └── /admin/users/[id]        (admin)
    ├── /admin/analytics             (admin)
    └── /admin/perks                 (admin)
```

---

## Route Table

| Path | Type | Auth | Purpose | Parent | Status |
|---|---|---|---|---|---|
| `/` | page | public | Landing — product intro, CTA to sign up | — | planned |
| `/signin` | page | public | Sign in with email + OTP (Better Auth) | `/` | planned |
| `/signup` | page | public | Create account | `/` | planned |
| `/share/[token]` | page | public | One-time share view — decryption key in URL fragment, no server involvement | `/` | planned |
| `/unlock` | page | authenticated | Master password unlock gate — derives vault key client-side before dashboard access | `/` | planned |
| `/onboarding` | page | authenticated | First-time vault setup — set master password, download emergency kit, install extension prompt | `/` | planned |
| `/dashboard` | page | authenticated | Vault overview — all items list, search, filter by type | `/` | planned |
| `/dashboard/[id]` | page | authenticated | View and edit individual vault item (login, card, note, SSH key, API key) | `/dashboard` | planned |
| `/dashboard/generator` | page | authenticated | Password generator — configurable length, charset, entropy display | `/dashboard` | planned |
| `/dashboard/watchtower` | page | authenticated | Breach detection (HIBP k-anonymity) + weak password analysis + reused password scan | `/dashboard` | planned |
| `/dashboard/sharing` | page | authenticated | Manage outgoing one-time encrypted share links — active, expired, revoked | `/dashboard` | planned |
| `/dashboard/circle` | page | authenticated | Trusted circle — invite friends, see who's on Sentri, manage circle members | `/dashboard` | planned |
| `/dashboard/perks` | page | authenticated | Affiliate perks and member discounts — browse, redeem, track | `/dashboard` | planned |
| `/dashboard/import` | page | authenticated | CSV import from Bitwarden, 1Password, LastPass, Chrome | `/dashboard` | planned |
| `/dashboard/emergency-kit` | page | authenticated | Generate and download emergency kit PDF — account recovery info | `/dashboard` | planned |
| `/dashboard/settings` | page | authenticated | Account settings — profile, email, preferences | `/dashboard` | planned |
| `/dashboard/settings/security` | page | authenticated | Security settings — change master password, 2FA setup | `/dashboard/settings` | planned |
| `/admin` | page | admin | Admin overview — key metrics, user count, signups over time, feature usage | `/` | planned |
| `/admin/users` | page | admin | Full user list — search, filter, sort by signup date / last active / item count / extension installed | `/admin` | planned |
| `/admin/users/[id]` | page | admin | Individual user detail — metadata only, never vault contents (zero-knowledge) | `/admin/users` | planned |
| `/admin/analytics` | page | admin | Growth charts — DAU/WAU/MAU, feature usage breakdown (generator, watchtower, sharing, perks, import) | `/admin` | planned |
| `/admin/perks` | page | admin | Manage perks and affiliate links — create, edit, deactivate, track redemptions | `/admin` | planned |

---

## Summary

- **Total routes:** 22
- **Public:** 4 (`/`, `/signin`, `/signup`, `/share/[token]`)
- **Authenticated:** 13 (`/unlock`, `/onboarding`, `/dashboard` and children)
- **Admin:** 5 (`/admin` and children)
- **No route group for admin beyond role guard** — Fahim is the only admin
- **Zero-knowledge note:** `/admin/users/[id]` exposes user metadata only. Vault contents are client-encrypted and invisible to the admin by design.

---

## Field Reference

- **Auth levels:** `public`, `authenticated`, `admin`
- **Status progression:** `planned` → `built` (repo-maintainer updates as routes are coded) → `verified` (Sentinel / valley-of-death, once auth gates confirmed against actual code)
- **API routes do not belong here.** Backend endpoints stay in PLANNER.md's "API Routes" table.

---

*Generated by tree-man on 2026-06-29*
